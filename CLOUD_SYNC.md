# 自托管云同步

AI Bro 已实现自托管同步首版，用于同一人的多个桌面工作区之间同步知识、任务和资料。你可以运行仓库附带的独立云服务；不需要购买或连接 AI Bro 官方云，也不依赖模型提供商的账号。部署与账号初始化步骤见 [云服务部署指南](cloud/README.md)。

这是本地优先的同步功能：不连接服务器也能保存和使用工作站。首版支持自动同步、手动同步、冲突处理和设备撤销；还没有端到端加密、S3 存储、按项目选择同步或实时多人协作。

## 在桌面端连接

1. 按 [部署指南](cloud/README.md) 启动独立服务并创建账号。公网服务使用 HTTPS；仅本机回环地址允许 HTTP 开发连接。服务地址不能包含账号密码、查询参数或片段，客户端也不会跟随重定向。
2. 在工作站“设置 → 账号与云同步”填写服务器地址、用户名、密码和本机设备名称。
3. 核对目标服务器，勾选“我确认将当前本地知识与云端合并”，再点击“连接并合并”。这会上传当前工作区支持同步的内容，并接收账号中已有内容。
4. 在另一台设备连接同一服务和账号，也要确认首次合并。没有官方账号注册流程；账号由自托管服务器的操作者创建。

首次合并不是以其中一台设备覆盖另一台。稳定 ID 相同的记录会按版本核对；冲突需要选择。**同名但不同 ID 的项目、笔记或附件不会自动视为同一条记录**，独立导入的数据仍可能同时保留。

一个本地工作区会绑定首次连接的服务器与账号。断开后仍保留这个绑定、同步游标和本地内容，不能通过改填另一个账号或地址把两套资料混在一起。连接其他目标需要使用独立本地工作区；首版尚无工作区迁移向导。

## 同步哪些内容

| 内容 | 首版行为 |
| --- | --- |
| 项目、任务、笔记、论文分析 | 同步记录、状态和支持的关联字段 |
| 附件原件与解析内容 | 同步受管原件、名称、内容与页信息；原件按 SHA-256 校验和去重 |
| 对话 | 同步已保存消息及支持的关联信息；发送中的运行状态和执行器状态留在本机 |
| 文件夹、关系、回收站、技能 | 同步支持的结构化记录；技能内容不会因此获得执行权限 |
| 论文 Markdown、导出目录 | 属于本机生成的派生文件，根据同步后的论文记录重新生成；不是任意目录同步 |

只改附件名称等元信息时，客户端会核验本机原件的实际哈希并复用匹配内容，避免重复下载。仅文件名相同或数据库声称哈希相同，不足以复用原件；冲突中的不同原件版本也不会互相替代。

下列配置和能力留在当前设备：模型 API Key、账号令牌、云会话凭据、本机目录路径与访问授权、对话权限模式、界面设置等。另一台设备需要自行配置模型服务，并重新授权本机项目目录。连接本机源码目录不会把整个目录自动上传，已经主动导入或写进笔记、对话的内容则属于同步内容。

同步字段过滤不等于任意文本的秘密识别：如果把密码或其他敏感文字直接写入笔记、对话或附件正文，它们仍可能作为内容同步。云服务可以读取同步内容明文，HTTPS 保护传输，不提供端到端加密。

## 自动同步与冲突

本机保存先写本地数据，网络同步由后台工作线程完成。关闭“自动同步”后，仍可点击“立即同步”；断网时本机内容和待上传操作会保留。重新连接后可以继续同步，不需要重新导入资料。

设置页会显示待同步数量、最近同步时间、同步中、已暂停、离线或冲突等状态。较大的资料库分批处理，存在待发送操作时不会标为全部同步完成。单附件上限为 64 MiB；结构化实体和请求也有大小限制，具体见 [协议限额](cloud/README.md#限额和边界)。超过限制会报错并保留本地数据。

后台收到更新时，本机同时保存的不同实体或不同字段可以通过三方合并保留。双方修改同一段笔记、同一字段，或一端删除而另一端修改时，系统不会拼接文本或静默覆盖。

在“处理冲突”中查看两边版本，再选择“保留本机”或“使用云端”。处理后按所选版本继续同步；未解决的记录会保留冲突。正在编辑时收到的更新可能暂缓显示，完成编辑后可使用“应用已收到的更新”。

删除也是同步操作。把内容移入回收站会同步它的活动状态与恢复记录；回收站永久删除不会清除所有历史、备份或服务器附件副本。云服务首版保留变更历史和删除标记，尚未提供账号销毁、历史保留策略或自动附件垃圾回收。

## 设备与凭据

“管理设备”可查看当前账号的设备，并撤销指定设备的后续访问。撤销后需要重新登录；**已经下载到那台设备的本地资料不会被远程擦除**。断开连接会清除当前设备保存的云会话，同时保留本地工作区。

密码只用于登录请求，不写入工作区。会话令牌由本地 Python 后端持有，不进入工作区快照、模型上下文或状态响应。若安装并可使用 macOS Keychain 的 `keyring` 后端，优先存入钥匙串；否则明确使用权限受限的本地文件：

```text
<本地数据目录>/cloud-sync/cloud-session.json
```

会话目录权限为 `0700`，文件权限为 `0600`。受限文件权限不等于加密；有该操作系统账号权限的人仍可能读取它。服务端只保存令牌哈希，会话过期或撤销后需要重新登录。

## 本地数据库与备份

本地 SQLite `workspace.sqlite3` 是规范数据源，保存实体、待同步队列、版本和冲突。`workspace.json` 是可读镜像，便于兼容和诊断；首次从旧 JSON 工作区迁移时会保留 `workspace.pre-sqlite.json`。不要把编辑镜像 JSON 当成修改 SQLite 工作区的方法，也不要把正在运行的数据库放进通用文件同步工具共享写入。

同步和备份解决不同问题：同步会传播修改与删除；备份用于找回某个历史时点。建议在应用停止写入后备份整个本地数据目录，或使用 SQLite 一致性备份并同时保存受管附件。不要只复制运行中的 `.sqlite3` 文件而遗漏 WAL。Markdown/JSON 导出也不一定包含全部原始附件、同步队列或恢复记录。

完整本地目录备份可能包含 `cloud-sync` 会话文件，共享或跨设备恢复备份时应排除会话凭据，并在目标设备重新登录。关联的本机项目源码位于工作区外，需要独立备份。云端备份需覆盖数据库、WAL 和附件卷，操作步骤见 [部署指南](cloud/README.md)。

## 当前不提供

- 官方托管账号服务、公开注册、OIDC 或企业单点登录。
- 端到端加密、S3/其他对象存储接入、按项目选择同步。
- 多人实时协作、CRDT、共享项目权限管理或云端运行 Agent。
- 任意本机目录同步、远程删除设备文件、跨设备继承源码访问权限。
- 云端历史自动清理、账号数据销毁、备份保留策略或完整灾难恢复向导。

本版面向个人自托管、多设备顺序或间歇使用。部署者需要维护服务、TLS、账号和备份；数据库版本冲突有保护，但它不是一个已完成全部运维能力的托管协作服务。
