# AI Bro website

Bilingual static product page. Serve `dist/` with an HTTP server; no build step is required. `?lang=en` selects English.

Feature media uses actual example-workspace recordings, with lightweight videos for viewport playback and downloadable GIFs. The English page uses a separate English recording and labeled workflow illustrations. Website deployment is managed by `.github/workflows/pages.yml`.
